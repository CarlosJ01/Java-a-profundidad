package Lambdas.custom;

public interface Calculable {
//	Interface funcional por que solo tiene un metodo abstarcto
//	...(Var args) puede resivir varias variables es un arreglo
	double avg(double ...values);
}
